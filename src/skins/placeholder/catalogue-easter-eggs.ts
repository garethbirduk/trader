import type { InsertItemKindInput } from "../../engine/stock/items-repo.js";

/**
 * Easter-egg item catalogue — data has moved to ./data/items.json
 * (or will live in its own file when we decide how to flag them).
 * Empty stub kept so index.ts still compiles. Loader TBD.
 */
export const EASTER_EGG_ITEMS: readonly InsertItemKindInput[] = [];
